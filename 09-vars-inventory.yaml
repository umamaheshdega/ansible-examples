- name: variables for inventory
  hosts: web
  # connection: local
  tasks:
  - name: print the information
    ansible.builtin.debug:
      msg: "COURSE IS {{ COURSE }}, DURATION IS {{ DURATION }}, TRAINER IS {{ TRAINER }}"