- name: install nginx in web servers
  hosts: web
  tasks: 
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name : start the nginx
    ansible.builtin.package:
      name: nginx
      state: started
      enabled: yes
          

